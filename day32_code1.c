// Question 63
// Merge two arrays.

#include <stdio.h>

int main() {
    int a[3] = {1, 2, 3};
    int b[3] = {4, 5, 6};

    for(int i = 0; i < 3; i++)
        printf("%d ", a[i]);

    for(int i = 0; i < 3; i++)
        printf("%d ", b[i]);

    return 0;
}